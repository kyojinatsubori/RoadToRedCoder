・訪問済み（黒）、訪問予定（灰）、未発見（白）の3状態を考える。
・グラフの表し方は隣接リスト表現が基本。（各ノードについて隣接するノードを記録）
・dist（最短距離を表す）、queue（訪問予定を格納）を用いる。
・dist == -1 を初期値とすることで、発見済みかも管理できる。
・計算量は頂点N辺Mとした時、O(N+M)
・最短経路アルゴリズムとして利用されることが多い。(DFSとの比較)

・各辺に重みがある場合はダイクストラ法