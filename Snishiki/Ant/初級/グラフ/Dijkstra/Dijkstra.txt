ダイクストラ法では重み付き無向・有向グラフにおける、
あるノードから各ノードへの最短距離を求めることができる。

計算量はO(N**2)で、priorityQueue(heapq)を用いることでO(NLogN)　O((E+V)logV)?となる。

ダイクストラではスタートから各ノードへの距離と各ノードに到達する一つ前のノードを記録する。

負の重み付き問題は解けない。

考え方としては、スタートからの距離が最短のノードの距離を確定する、を繰り返す。